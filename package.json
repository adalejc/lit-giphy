{
  "scripts": {
    "build": "rimraf dist && rollup -c rollup.config.js",
    "start:build": "npm run build && web-dev-server --root-dir dist --app-index index.html --open --compatibility none",
    "start": "web-dev-server"
  },
  "devDependencies": {
    "@open-wc/building-rollup": "^1.9.4",
    "deepmerge": "^4.2.2",
    "rimraf": "^3.0.2",
    "rollup": "^2.44.0",
    "@web/dev-server": "^0.1.10"
  },
  "name": "lit-giphy",
  "version": "0.0.0",
  "description": "Webcomponent lit-giphy following open-wc recommendations",
  "author": "lit-giphy",
  "license": "MIT",
  "dependencies": {
    "lit-html": "^1.3.0",
    "lit-element": "^2.4.0"
  }
}
